#define BOOST_UT_INTERFACE
#include <boost/ut.hpp>

namespace ut = boost::ut;

static ut::suite _ = [] {
  using namespace ut;

  <% (0..n).each do |i| %>
    "test_suite_<%= v %>.<%= i %>"_test = [] {
      expect(42_i == 42);
      expect(43_i == 43);
		};
  <% end %>
};
