#define CATCH_CONFIG_FAST_COMPILE
#define CATCH_CONFIG_RUNNER
#include <catch.hpp>

<% (0..n).each do |i| %>
  TEST_CASE("<%= v %>.<%= i %>") {
    REQUIRE(42 == 42);
    REQUIRE(43 == 43);
  }
<% end %>
