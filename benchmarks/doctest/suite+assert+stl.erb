#define DOCTEST_CONFIG_IMPLEMENTATION_IN_DLL
#define DOCTEST_CONFIG_SUPER_FAST_ASSERTS
#include <doctest.h>
#include <string>
#include <vector>

<% (1..n).each do |i| %>
  TEST_CASE("<%= v %>.<%= i %>") {
    CHECK(std::string{"string"} == std::string{"string"});
    CHECK(std::vector{1, 2, 3} == std::vector{1, 2, 3});
  }
<% end %>
