#define CATCH_CONFIG_FAST_COMPILE
#include <catch.hpp>

TEST_CASE("suite+assert") {
  <% (1..n).each do |i| %>
    SECTION("<%= v %>.<%= i %>") {
      CHECK(true == true);
      CHECK(42 == 42);
      CHECK(1 != 2);
      CHECK(1.23f == Approx(1.23));
    }
  <% end %>
}
