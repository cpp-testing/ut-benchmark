#define CATCH_CONFIG_FAST_COMPILE
#include <catch.hpp>

<% (1..n).each do |i| %>
  TEST_CASE("<%= v %>.<%= i %>") {
    CHECK(true == true);
    CHECK(42 == 42);
    CHECK(1 != 2);
    CHECK(1.23f == Approx(1.23));
  }
<% end %>
