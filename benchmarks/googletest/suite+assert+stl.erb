#include <gtest/gtest.h>
#include <string>
#include <vector>

class SuiteAssertStl : public ::testing::Test { };

<% (1..n).each do |i| %>
  TEST_F(SuiteAssertStl, Test<%= v %><%= i %>) {
    EXPECT_EQ(std::string{"string"}, std::string{"string"});
    EXPECT_EQ((std::vector{1, 2, 3}), (std::vector{1, 2, 3}));
  }
<% end %>
