#include <gtest/gtest.h>

class SuiteAssert : public ::testing::Test { };

<% (1..n).each do |i| %>
  TEST_F(SuiteAssert, Test<%= v %><%= i %>) {
    EXPECT_EQ(true, true);
    EXPECT_EQ(42, 42);
    EXPECT_NE(1, 2);
    EXPECT_NEAR(1.23f, 1.23f, 0.1);
  }
<% end %>
