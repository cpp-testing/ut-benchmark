#include <gtest/gtest.h>

<% (0..n).each do |i| %>
  TEST(Suite<%= v %>, Test<%= i %>) {
    EXPECT_EQ(true, true);
    EXPECT_EQ(42, 42);
    EXPECT_NE(1, 2);
    EXPECT_NEAR(1.23f, 1.23f, 0.1);
  }
<% end %>
